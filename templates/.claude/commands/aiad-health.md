---
name: aiad-health
description: alias rétro-compatible → /aiad health (déprécié, retiré en v2)
---

⚠ `/aiad-health` est un **alias rétro-compatible déprécié**. Il sera retiré à la v2. Préfère désormais `/aiad health`.

Affiche d'abord à l'utilisateur, en une seule ligne :

> ℹ Migration : `/aiad-health` → `/aiad health` (les alias plats seront retirés en v2).

Puis charge `.claude/aiad/health.md` avec ton outil `Read` et **suis ses instructions à la lettre** en lui passant `$ARGUMENTS`.

$ARGUMENTS
