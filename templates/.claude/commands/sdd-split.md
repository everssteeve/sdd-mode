---
name: sdd-split
description: alias rétro-compatible → /sdd split (déprécié, retiré en v2)
---

⚠ `/sdd-split` est un **alias rétro-compatible déprécié**. Il sera retiré à la v2. Préfère désormais `/sdd split`.

Affiche d'abord à l'utilisateur, en une seule ligne :

> ℹ Migration : `/sdd-split` → `/sdd split` (les alias plats seront retirés en v2).

Puis charge `.claude/sdd/split.md` avec ton outil `Read` et **suis ses instructions à la lettre** en lui passant `$ARGUMENTS`.

$ARGUMENTS
