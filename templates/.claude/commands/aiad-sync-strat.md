---
name: aiad-sync-strat
description: alias rétro-compatible → /aiad sync-strat (déprécié, retiré en v2)
---

⚠ `/aiad-sync-strat` est un **alias rétro-compatible déprécié**. Il sera retiré à la v2. Préfère désormais `/aiad sync-strat`.

Affiche d'abord à l'utilisateur, en une seule ligne :

> ℹ Migration : `/aiad-sync-strat` → `/aiad sync-strat` (les alias plats seront retirés en v2).

Puis charge `.claude/aiad/sync-strat.md` avec ton outil `Read` et **suis ses instructions à la lettre** en lui passant `$ARGUMENTS`.

$ARGUMENTS
