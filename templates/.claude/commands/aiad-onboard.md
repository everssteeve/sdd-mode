---
name: aiad-onboard
description: alias rétro-compatible → /aiad onboard (déprécié, retiré en v2)
---

⚠ `/aiad-onboard` est un **alias rétro-compatible déprécié**. Il sera retiré à la v2. Préfère désormais `/aiad onboard`.

Affiche d'abord à l'utilisateur, en une seule ligne :

> ℹ Migration : `/aiad-onboard` → `/aiad onboard` (les alias plats seront retirés en v2).

Puis charge `.claude/aiad/onboard.md` avec ton outil `Read` et **suis ses instructions à la lettre** en lui passant `$ARGUMENTS`.

$ARGUMENTS
