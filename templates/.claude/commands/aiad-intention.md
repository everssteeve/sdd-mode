---
name: aiad-intention
description: alias rétro-compatible → /aiad intention (déprécié, retiré en v2)
---

⚠ `/aiad-intention` est un **alias rétro-compatible déprécié**. Il sera retiré à la v2. Préfère désormais `/aiad intention`.

Affiche d'abord à l'utilisateur, en une seule ligne :

> ℹ Migration : `/aiad-intention` → `/aiad intention` (les alias plats seront retirés en v2).

Puis charge `.claude/aiad/intention.md` avec ton outil `Read` et **suis ses instructions à la lettre** en lui passant `$ARGUMENTS`.

$ARGUMENTS
