---
name: sdd-audit
description: alias rétro-compatible → /sdd audit (déprécié, retiré en v2)
---

⚠ `/sdd-audit` est un **alias rétro-compatible déprécié**. Il sera retiré à la v2. Préfère désormais `/sdd audit`.

Affiche d'abord à l'utilisateur, en une seule ligne :

> ℹ Migration : `/sdd-audit` → `/sdd audit` (les alias plats seront retirés en v2).

Puis charge `.claude/sdd/audit.md` avec ton outil `Read` et **suis ses instructions à la lettre** en lui passant `$ARGUMENTS`.

$ARGUMENTS
