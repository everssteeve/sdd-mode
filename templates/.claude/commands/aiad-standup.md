---
name: aiad-standup
description: alias rétro-compatible → /aiad standup (déprécié, retiré en v2)
---

⚠ `/aiad-standup` est un **alias rétro-compatible déprécié**. Il sera retiré à la v2. Préfère désormais `/aiad standup`.

Affiche d'abord à l'utilisateur, en une seule ligne :

> ℹ Migration : `/aiad-standup` → `/aiad standup` (les alias plats seront retirés en v2).

Puis charge `.claude/aiad/standup.md` avec ton outil `Read` et **suis ses instructions à la lettre** en lui passant `$ARGUMENTS`.

$ARGUMENTS
