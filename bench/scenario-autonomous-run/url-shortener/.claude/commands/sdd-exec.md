---
name: sdd-exec
description: alias rétro-compatible → /sdd exec (déprécié, retiré en v2)
---

⚠ `/sdd-exec` est un **alias rétro-compatible déprécié**. Il sera retiré à la v2. Préfère désormais `/sdd exec`.

Affiche d'abord à l'utilisateur, en une seule ligne :

> ℹ Migration : `/sdd-exec` → `/sdd exec` (les alias plats seront retirés en v2).

Puis charge `.claude/sdd/exec.md` avec ton outil `Read` et **suis ses instructions à la lettre** en lui passant `$ARGUMENTS`.

$ARGUMENTS
