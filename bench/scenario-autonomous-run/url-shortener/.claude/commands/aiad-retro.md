---
name: aiad-retro
description: alias rétro-compatible → /aiad retro (déprécié, retiré en v2)
---

⚠ `/aiad-retro` est un **alias rétro-compatible déprécié**. Il sera retiré à la v2. Préfère désormais `/aiad retro`.

Affiche d'abord à l'utilisateur, en une seule ligne :

> ℹ Migration : `/aiad-retro` → `/aiad retro` (les alias plats seront retirés en v2).

Puis charge `.claude/aiad/retro.md` avec ton outil `Read` et **suis ses instructions à la lettre** en lui passant `$ARGUMENTS`.

$ARGUMENTS
