---
name: sdd-fact
description: alias rétro-compatible → /sdd fact (déprécié, retiré en v2)
---

⚠ `/sdd-fact` est un **alias rétro-compatible déprécié**. Il sera retiré à la v2. Préfère désormais `/sdd fact`.

Affiche d'abord à l'utilisateur, en une seule ligne :

> ℹ Migration : `/sdd-fact` → `/sdd fact` (les alias plats seront retirés en v2).

Puis charge `.claude/sdd/fact.md` avec ton outil `Read` et **suis ses instructions à la lettre** en lui passant `$ARGUMENTS`.

$ARGUMENTS
