---
name: aiad-init
description: alias rétro-compatible → /aiad init (déprécié, retiré en v2)
---

⚠ `/aiad-init` est un **alias rétro-compatible déprécié**. Il sera retiré à la v2. Préfère désormais `/aiad init`.

Affiche d'abord à l'utilisateur, en une seule ligne :

> ℹ Migration : `/aiad-init` → `/aiad init` (les alias plats seront retirés en v2).

Puis charge `.claude/aiad/init.md` avec ton outil `Read` et **suis ses instructions à la lettre** en lui passant `$ARGUMENTS`.

$ARGUMENTS
