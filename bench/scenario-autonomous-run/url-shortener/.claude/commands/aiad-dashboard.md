---
name: aiad-dashboard
description: alias rétro-compatible → /aiad dashboard (déprécié, retiré en v2)
---

⚠ `/aiad-dashboard` est un **alias rétro-compatible déprécié**. Il sera retiré à la v2. Préfère désormais `/aiad dashboard`.

Affiche d'abord à l'utilisateur, en une seule ligne :

> ℹ Migration : `/aiad-dashboard` → `/aiad dashboard` (les alias plats seront retirés en v2).

Puis charge `.claude/aiad/dashboard.md` avec ton outil `Read` et **suis ses instructions à la lettre** en lui passant `$ARGUMENTS`.

$ARGUMENTS
