---
name: aiad-demo
description: alias rétro-compatible → /aiad demo (déprécié, retiré en v2)
---

⚠ `/aiad-demo` est un **alias rétro-compatible déprécié**. Il sera retiré à la v2. Préfère désormais `/aiad demo`.

Affiche d'abord à l'utilisateur, en une seule ligne :

> ℹ Migration : `/aiad-demo` → `/aiad demo` (les alias plats seront retirés en v2).

Puis charge `.claude/aiad/demo.md` avec ton outil `Read` et **suis ses instructions à la lettre** en lui passant `$ARGUMENTS`.

$ARGUMENTS
