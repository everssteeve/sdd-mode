---
name: sdd-trace
description: alias rétro-compatible → /sdd trace (déprécié, retiré en v2)
---

⚠ `/sdd-trace` est un **alias rétro-compatible déprécié**. Il sera retiré à la v2. Préfère désormais `/sdd trace`.

Affiche d'abord à l'utilisateur, en une seule ligne :

> ℹ Migration : `/sdd-trace` → `/sdd trace` (les alias plats seront retirés en v2).

Puis charge `.claude/sdd/trace.md` avec ton outil `Read` et **suis ses instructions à la lettre** en lui passant `$ARGUMENTS`.

$ARGUMENTS
