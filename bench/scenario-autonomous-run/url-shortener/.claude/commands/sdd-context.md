---
name: sdd-context
description: alias rétro-compatible → /sdd context (déprécié, retiré en v2)
---

⚠ `/sdd-context` est un **alias rétro-compatible déprécié**. Il sera retiré à la v2. Préfère désormais `/sdd context`.

Affiche d'abord à l'utilisateur, en une seule ligne :

> ℹ Migration : `/sdd-context` → `/sdd context` (les alias plats seront retirés en v2).

Puis charge `.claude/sdd/context.md` avec ton outil `Read` et **suis ses instructions à la lettre** en lui passant `$ARGUMENTS`.

$ARGUMENTS
