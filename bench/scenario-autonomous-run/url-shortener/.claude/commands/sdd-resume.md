---
name: sdd-resume
description: alias rétro-compatible → /sdd resume (déprécié, retiré en v2)
---

⚠ `/sdd-resume` est un **alias rétro-compatible déprécié**. Il sera retiré à la v2. Préfère désormais `/sdd resume`.

Affiche d'abord à l'utilisateur, en une seule ligne :

> ℹ Migration : `/sdd-resume` → `/sdd resume` (les alias plats seront retirés en v2).

Puis charge `.claude/sdd/resume.md` avec ton outil `Read` et **suis ses instructions à la lettre** en lui passant `$ARGUMENTS`.

$ARGUMENTS
