---
name: aiad-status
description: alias rétro-compatible → /aiad status (déprécié, retiré en v2)
---

⚠ `/aiad-status` est un **alias rétro-compatible déprécié**. Il sera retiré à la v2. Préfère désormais `/aiad status`.

Affiche d'abord à l'utilisateur, en une seule ligne :

> ℹ Migration : `/aiad-status` → `/aiad status` (les alias plats seront retirés en v2).

Puis charge `.claude/aiad/status.md` avec ton outil `Read` et **suis ses instructions à la lettre** en lui passant `$ARGUMENTS`.

$ARGUMENTS
