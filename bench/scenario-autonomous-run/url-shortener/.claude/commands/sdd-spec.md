---
name: sdd-spec
description: alias rétro-compatible → /sdd spec (déprécié, retiré en v2)
---

⚠ `/sdd-spec` est un **alias rétro-compatible déprécié**. Il sera retiré à la v2. Préfère désormais `/sdd spec`.

Affiche d'abord à l'utilisateur, en une seule ligne :

> ℹ Migration : `/sdd-spec` → `/sdd spec` (les alias plats seront retirés en v2).

Puis charge `.claude/sdd/spec.md` avec ton outil `Read` et **suis ses instructions à la lettre** en lui passant `$ARGUMENTS`.

$ARGUMENTS
