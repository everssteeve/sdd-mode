---
name: sdd-init
description: alias rétro-compatible → /sdd init (déprécié, retiré en v2)
---

⚠ `/sdd-init` est un **alias rétro-compatible déprécié**. Il sera retiré à la v2. Préfère désormais `/sdd init`.

Affiche d'abord à l'utilisateur, en une seule ligne :

> ℹ Migration : `/sdd-init` → `/sdd init` (les alias plats seront retirés en v2).

Puis charge `.claude/sdd/init.md` avec ton outil `Read` et **suis ses instructions à la lettre** en lui passant `$ARGUMENTS`.

$ARGUMENTS
