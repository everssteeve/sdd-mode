---
name: sdd-gate
description: alias rétro-compatible → /sdd gate (déprécié, retiré en v2)
---

⚠ `/sdd-gate` est un **alias rétro-compatible déprécié**. Il sera retiré à la v2. Préfère désormais `/sdd gate`.

Affiche d'abord à l'utilisateur, en une seule ligne :

> ℹ Migration : `/sdd-gate` → `/sdd gate` (les alias plats seront retirés en v2).

Puis charge `.claude/sdd/gate.md` avec ton outil `Read` et **suis ses instructions à la lettre** en lui passant `$ARGUMENTS`.

$ARGUMENTS
