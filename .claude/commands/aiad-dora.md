---
name: aiad-dora
description: alias rétro-compatible → /aiad dora (déprécié, retiré en v2)
---

⚠ `/aiad-dora` est un **alias rétro-compatible déprécié**. Il sera retiré à la v2. Préfère désormais `/aiad dora`.

Affiche d'abord à l'utilisateur, en une seule ligne :

> ℹ Migration : `/aiad-dora` → `/aiad dora` (les alias plats seront retirés en v2).

Puis charge `.claude/aiad/dora.md` avec ton outil `Read` et **suis ses instructions à la lettre** en lui passant `$ARGUMENTS`.

$ARGUMENTS
