---
name: aiad-flow
description: alias rétro-compatible → /aiad flow (déprécié, retiré en v2)
---

⚠ `/aiad-flow` est un **alias rétro-compatible déprécié**. Il sera retiré à la v2. Préfère désormais `/aiad flow`.

Affiche d'abord à l'utilisateur, en une seule ligne :

> ℹ Migration : `/aiad-flow` → `/aiad flow` (les alias plats seront retirés en v2).

Puis charge `.claude/aiad/flow.md` avec ton outil `Read` et **suis ses instructions à la lettre** en lui passant `$ARGUMENTS`.

$ARGUMENTS
