---
name: sdd-security
description: alias rétro-compatible → /sdd security (déprécié, retiré en v2)
---

⚠ `/sdd-security` est un **alias rétro-compatible déprécié**. Il sera retiré à la v2. Préfère désormais `/sdd security`.

Affiche d'abord à l'utilisateur, en une seule ligne :

> ℹ Migration : `/sdd-security` → `/sdd security` (les alias plats seront retirés en v2).

Puis charge `.claude/sdd/security.md` avec ton outil `Read` et **suis ses instructions à la lettre** en lui passant `$ARGUMENTS`.

$ARGUMENTS
