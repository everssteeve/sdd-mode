---
name: sdd-validate
description: alias rétro-compatible → /sdd validate (déprécié, retiré en v2)
---

⚠ `/sdd-validate` est un **alias rétro-compatible déprécié**. Il sera retiré à la v2. Préfère désormais `/sdd validate`.

Affiche d'abord à l'utilisateur, en une seule ligne :

> ℹ Migration : `/sdd-validate` → `/sdd validate` (les alias plats seront retirés en v2).

Puis charge `.claude/sdd/validate.md` avec ton outil `Read` et **suis ses instructions à la lettre** en lui passant `$ARGUMENTS`.

$ARGUMENTS
