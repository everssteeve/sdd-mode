---
name: sdd-intent
description: alias rétro-compatible → /sdd intent (déprécié, retiré en v2)
---

⚠ `/sdd-intent` est un **alias rétro-compatible déprécié**. Il sera retiré à la v2. Préfère désormais `/sdd intent`.

Affiche d'abord à l'utilisateur, en une seule ligne :

> ℹ Migration : `/sdd-intent` → `/sdd intent` (les alias plats seront retirés en v2).

Puis charge `.claude/sdd/intent.md` avec ton outil `Read` et **suis ses instructions à la lettre** en lui passant `$ARGUMENTS`.

$ARGUMENTS
