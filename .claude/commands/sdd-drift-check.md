---
name: sdd-drift-check
description: alias rétro-compatible → /sdd drift-check (déprécié, retiré en v2)
---

⚠ `/sdd-drift-check` est un **alias rétro-compatible déprécié**. Il sera retiré à la v2. Préfère désormais `/sdd drift-check`.

Affiche d'abord à l'utilisateur, en une seule ligne :

> ℹ Migration : `/sdd-drift-check` → `/sdd drift-check` (les alias plats seront retirés en v2).

Puis charge `.claude/sdd/drift-check.md` avec ton outil `Read` et **suis ses instructions à la lettre** en lui passant `$ARGUMENTS`.

$ARGUMENTS
